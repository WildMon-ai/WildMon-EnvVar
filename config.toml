GEE_PROJECT_ID = "wildmon-projects"
LOCATIONS_CSV_PATH = "./input/configuration.csv"
OUTPUT_CSV_PATH = "output/env_vars.csv"
LAT_COLUMN_NAME = "latitude"
LON_COLUMN_NAME = "longitude"
SAMPLING_POINT_BUFFER_METERS = 200
AOI_BUFFER_KM = 30
IMAGE_START_DATE = "2025-01-01"
IMAGE_END_DATE = "2025-12-31"
MAX_SEARCH_DISTANCE_M_WATERDIST = 20000

# Optional: authenticate with a service account instead of the browser flow
# SERVICE_ACCOUNT = "service-account@google-cloud-project.iam.gserviceaccount.com"
# SERVICE_ACCOUNT_KEY_FILE = "/abs/path/to/service-account-key.json"

# Optional: WorldClim variables (comment out to use all)
# WORLDCLIM_VARIABLES = ["bio01", "bio12"]
